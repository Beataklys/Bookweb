<h1 class="current-category"><%= params[:category]%></h1>
<% if @books.count ==0 %>
<h1> There are no books currently in this category <h2>
  <% else %>
<% @books.each do |book| %>
  <h2> Title: <%= link_to book.title, book_path(book) %></h1>
<% end %>
<% end %>
